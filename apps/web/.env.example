# Server-only, never prefix this with NEXT_PUBLIC_. Use the read-only application role.
DATABASE_URL=postgresql://agro_reader:REPLACE_ME@SERVER.postgres.database.azure.com:5432/agroamigo
# TLS certificate verification is required by default. Only for a local PostgreSQL instance:
# DATABASE_SSL=false

# Optional commercial Open-Meteo plan. Never expose this key to the browser.
# OPEN_METEO_API_KEY=
