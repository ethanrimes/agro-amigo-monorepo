// Hand-written test placeholder so `flutter create .` doesn't regenerate a
// broken `MyApp` widget_test on CI. Keep the test trivial; the real coverage
// happens in screen-level smoke tests added in follow-up commits.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(2 + 2, 4);
  });
}
