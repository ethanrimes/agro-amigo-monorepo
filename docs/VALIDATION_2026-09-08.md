# Current delivery and validation

Validation recorded on 8 September 2026 UTC (7–8 September in Colombia). This report separates deployed behavior, completed checks, and the remaining unattended archive queue. Earlier deployment evidence is retained in [the initial validation report](INGESTION_VALIDATION_2026-09-07.md).

## Cloud and retention

The service uses subscription `9a04b64b-af19-4519-be50-56ec2acbd855` and the existing AgroAmigo PostgreSQL database. The web app is [AgroAmigo](https://agroamigo-demo-9a04.azurewebsites.net); the recurring worker is `agroamigo-data-9a04`.

- The shared App Service plan was increased from B1 to B2 (two cores / 3.5 GB) after the real large-file cloud run exposed contention. Provisioning preserves an existing SKU unless explicitly overridden. The PostgreSQL server is unchanged. [Resource evidence](../artifacts/ingestion-plan-b2-update.json).
- Daily refresh: 23:00 UTC / 18:00 Colombia. Historical backfill: hourly at minute 15. Both monitored triggers are enabled; Always On is enabled.
- Originals are hashed before parsing and retained in the private `agroamigodata9a04/source-archive` container and database. Changed content at an existing URL creates a new document and retained observation revision.
- All stored PDF, Excel, ZIP and CSV originals were verified to have corresponding Blobs. Downloaded ZIP, individual city PDF, XLS/XLSX, CSV and pork PDF samples match their original hashes and byte sizes.
- Retention/immutability triggers are enabled, no Blob lifecycle deletion policy is configured, and the ingestion role lacks DELETE/TRUNCATE privileges.
- Runtime configuration and Gemini credentials are stored in Azure app settings. Deployment preserves existing operator settings; source keys are excluded from packages and Git.
- Final worker release: `73c19706beeb4e184ad369d64def4ef93a6c0e4e26e16e4e958d4c4e08202eae`. The final large-file run successfully published 33,981 monthly groups in 304.4 seconds with zero errors (details below).
- Earlier cloud run `9de51a9e-50cb-4d20-b528-46cb1ef5cb49` succeeded: three assets, 2,483 published observations, zero errors, 120.3 seconds. All 49 new originals (ZIP plus 48 PDFs) passed Blob/hash checks. The run honored its bounded execution deadline before starting another asset. Earlier run `06a4db7b-c60d-4ad0-84b4-7f640892d278` independently verified 1,956 prices and 33 retained originals.
- Worker health now uses bounded queries; historical coverage is opt-in. At 03:38 UTC, live health returned HTTP 200 in 0.415s; DANE and World Bank workbook previews returned HTTP 200 in 0.548s and 0.441s.

At 05:00:05 UTC the queue had 500 completed assets, 76 archived documents, 9,337 pending assets, one failed URL, three source-date reviews, one asset awaiting OCR and one processed asset awaiting its next stage. There were 1,464 retained source documents, including 1,084 originals. The scheduled hourly run finished normally after 35m31.7s: 29 assets and 1,951,636 processed rows. Two newly encountered PDF parser gaps were subsequently repaired and replayed successfully. Daily and hourly triggers remain enabled. Final status returned HTTP 200 in 0.456s; DANE and World Bank workbook previews returned HTTP 200 in 0.838s and 0.548s. [Final live checkpoint](../artifacts/final-delivery-ingestion-state.json). These are dated counts; the entire historical archive is still processing in Azure.

## Source coverage and extraction

Adapters cover all requested DANE source families: departmental and municipal insumos, ancillary production factors and electricity, daily workbooks, city PDFs inside ZIPs, monthly wholesale series and annexes, raw milk, rice and mill byproducts, and supply. DANE observations reach 2012 where available; rice starts in 2013. Published tables preserve category hierarchy, package sizes, units, location, reporting round and exact source locators.

Additional primary-source adapters cover UPRA/AgroNET cacao, Corabastos package/quality quotes, Fedegán cattle/meat series, Porkcolombia reports, Fedepalma FFP regulatory bases, USDA AMS flower quotes, and World Bank commodity benchmarks. See [Colombian sources](OFFICIAL_COLOMBIA_SOURCES.md) and [international sources](OFFICIAL_INTERNATIONAL_SOURCES.md) for official URLs and limitations. Domestic quotes, regulatory bases and international USD benchmarks retain separate price bases and units. A Miami flower report is not automatically labeled as Colombian origin.

At the 05:00 UTC checkpoint, 40,305 additional official observations and 605 distinct quote identities were published. World Bank's 31,381 observations cover January 1960–August 2026; AgroNET has 262 weekly cacao prices; Fedegán has 5,734 observations. These are dated counts and increase as the queue runs.

Completed checks:

- 124 final checks pass (122 ingestion and two infrastructure capacity checks), including seven tests built from real PDF objects with native headings, image-only tables, mixed native/image pages and harmless logos. A later validation found shared row locators across Porkcolombia market columns; the corrected adapter assigns each market its own locator and rejects collisions. Replaying the archived PDFs increased published pork observations from 282 to 726, adding 444 missing prices, with PDF pages preserved.

- Stress extraction of 92 real files: 3,299,355 parsed rows and no unexpected errors. The incorrect December 2020 milk workbook is rejected by date; the valid December PDF supplies that month. A missing publisher heading in the August 2012 inputs PDF is preserved for review rather than guessed.
- Native extraction runs before OCR. Actual scanned PDF and image-only Excel fallback was tested with two independent Gemini readings. Four commercial input records matched the native Excel identities in a rolled-back publication fixture. The final mixed-page regression additionally uses two real Gemini 3.5 Flash readings: both agree on the image table and the reconstructed document preserves both pages and COP 85,000–87,000 per 24 kg. No synthetic fixture prices were published. Four real current city PDFs retain their identical 323 observations with zero OCR tasks. Parser upgrades make already completed mixed documents eligible for non-destructive replay.
- A scanned Porkcolombia report supplied 108 validated observations from January–March 2020. Rendered pages and both readings are retained.
- Final queue audit found two valid milk workbooks whose multi-row department/municipality headers had been skipped. The corrected parser recovered 208 observations for November 2022 and 208 for December 2022, each spanning 25 departments. Both files were replayed with the restricted role; the public farmgate history API returns the exact Abejorral values (COP 1,979.10 and 2,037.66/litre) under the requested market and all-history filters. [Milk replay](../artifacts/milk-2022-layout/replay.json), [frontend proof](../artifacts/milk-2022-layout/frontend-api.json).
- The 95 MB 2020 supply workbook used day/month/two-digit-year text dates. The fixed streaming parser validates the century against the sheet's printed reporting year. An independent full-file reconciliation matched 1,813,661 source rows to 33,981 monthly groups, 29 markets, 177 foods, all twelve months and 6,308,398,393.95 kg. Thirty-three focused supply checks pass, including preserved row ranges, sparse worksheet rows, row formatting cleanup and 1904 Excel dates. [Supply reconciliation](../artifacts/supply-2020-validation.json). The full real-source cloud replay exposed excessive memory in the OCR pre-scan and openpyxl row formatting state. The corrected pre-scan uses 147 MB peak and 0.03s with zero OCR images; supply parsing dropped from 996.1 MB to 232.7 MB peak (77% reduction). All 33,981 groups and 1,662,791 source-range pairs retain an identical content digest. [Memory reconciliation](../artifacts/supply-memory-after.json), [pre-scan proof](../artifacts/supply-2020-ocr-prescan-memory.json). Actual Azure run `1194ade3-f1ad-4603-b84b-1f2ebe1d57e2` succeeded in 304.4s on B2: one complete source asset, 33,981 published groups and zero errors. All 95,409,078 original bytes were downloaded from Blob and hash/size verified. The diagnostic HTTP request timed out while processing continued; success was independently verified from the persistent run, asset status and published totals. [Cloud evidence](../artifacts/supply-2020-cloud-verification.json). The highest one-minute Azure app memory sample during this successful run was 402.5 MiB; this is a sampled metric, not an exact process peak. [Cloud memory samples](../artifacts/supply-2020-cloud-memory-final.json). The live public API returns January 2020 Armenia Mercar data: 109 rows totaling 10,508,613 kg, with 153 selectable historical months.
- Final scheduled discovery found three older USDA Miami layout variations and a milk PDF whose publication masthead differs from its observation month. Native extraction now handles the older USDA market boundary, wrapped price ranges and no-quote notices without interpreting narrative numbers as prices. Real July 28 / August 4 / August 11, 2025 reports reconciled every full and “mostly” range and published 61 / 63 / 62 quotes through restricted cloud replays; six existing source baselines stayed unchanged. Exact public API values and all three Blob hashes passed. [USDA cloud proof](../artifacts/usda-miami-cloud-verification.json). The milk PDF explicitly labels August 2014 in its price-table captions despite an October publication date. All 183 native PDF prices across 22 departments were retained, with unchanged values and source locators; four previously uncovered canonical prices were filled while preferred consolidated Excel observations remained. The public API returns the exact PDF sample, Donmatías COP 876/litre, and the original Blob hash matches. [Milk cloud proof](../artifacts/milk-2014-cloud-verification.json). Neither format needed OCR.
- Same-URL conditional HTTP tests cover 304 responses, changed bytes, parser upgrades and immutable originals. PostgreSQL queue tests cover source-file/index fairness, updated parser eligibility, null-date discoveries, stale review files and cooldowns.
- Adapter result keys include the adapter version; PDF page locators survive publication. Invalid source cells remain reviewable. Source-family discovery failures do not prevent unrelated queued work.

The only remaining failed URL is DANE's October 23, 2012 daily XLS, which still returns HTTP 404. Its retained companion PDF is a narrative bulletin and cannot reconstruct the missing matrix. Three documents remain in review because their printed dates conflict with their filenames; they are retained without guessing their dates. [Publisher recovery investigation](../artifacts/daily-2012-10-23-source-recovery.json).

Three OCR tasks also remain for review: two Corabastos pages have disagreeing or uncertain readings, and one embedded image on a DANE methodology sheet uses unsupported encoding. That methodology image is not a verified missing price table. Originals and both available readings remain retained.

## Application changes

The unified product catalog brings the previously separate official references, city-report products and monthly summaries into one search, grid and filtering experience. The verified source snapshot contains 1,029 price selections: 366 canonical product cards, 569 exact official quote identities and 94 standalone monthly-summary quotes. These are price selections, not 1,029 distinct product names. All 351 retained city names are searchable. All 457 monthly-summary quotes remain reachable: 363 quotes through 50 canonical product detail pages and 94 through standalone entries. Thirteen footnoted product names remain literal instead of being assigned guessed varieties. [Source reconciliation](../artifacts/unified-catalog-source-validation.json).

Cards show currency, unit, date, price basis and the market where it distinguishes a quote. Search accepts common rose/arabica variants. Category, currency, department and search filters stay visible; source details and original-document access are on the detail page. Exact card links preserve series, presentation, units, history and department, and the return link restores catalog filters. Old saved product IDs survive alongside distinct saved official quote identities. References without a verified department are shown with no department filter; the regional exclusion is explained. Official quote IDs are excluded from map pickers that do not support them. The old source-list URLs remain available for existing links.

The catalog measured 2.414s uncached and 0.078s cached in the local API against the live database, with a 979,690-byte response. Eight simultaneous uncached requests shared one eight-query batch and completed in 2.696s. Cache keys and lifetime are bounded. TypeScript, exact source/API value checks, saved-item/back-navigation tests and 320px browser layouts passed. [Cache and complete summary reachability](../artifacts/unified-catalog-service-validation.json), [API regression](../artifacts/unified-catalog-api-tests-final.log), [browser follow-up](../artifacts/unified-catalog-browser-followup.log).

Production web release `e34a99b1c3df421b8a771d8a908b615f` was built at 04:44:00 UTC. Deployment, release identity and database health were verified; no remote settings changed. Fingerprints confirm the checked application inputs stayed unchanged through build and deployment. Live catalog: 1,041 unique selections in 1.879s; January 2020 supply: 153 historical months, 109 rows, 10,508,613 kg. [Final live checks](../artifacts/web-unified-final-live-checks.json). At 05:10:30 UTC the catalog had grown to 1,103 unique selections (366 canonical and 737 official/summary references) as the archive replays published additional quotes; the unchanged release returned them in 2.846s. [Dated catalog checkpoint](../artifacts/unified-catalog-delivery-checkpoint.json).

Supply screens expose all retained months directly through one month selector and visible entity/month/source filters. The query always requests the complete retained history. The long chart scrolls horizontally with year labels. Historical supply and price evidence requests preserve selected older source records. Four final desktop/mobile checks selected all 24 months in 2019/2020, reconciled every displayed quantity and source link, resolved actual source records, and tested historical-month error recovery and 320px control width. [All-month validation](../artifacts/supply-all-months-validation.md).

Home autocomplete now resolves the exact unified catalog URL for both official quotes and city package selections. Four desktop/mobile checks verified those links and navigation when photographs fail; an early image failure before hydration now receives the same icon fallback. [Home catalog checks](../artifacts/home-unified-catalog-final-tests.log). Farm budget crop pickers accept only canonical monthly COP/kg selections, and FNC retains its explicit 125 kg conversion. International benchmarks and package quotes cannot silently enter kilogram revenue calculations. Four compatibility checks cover both budget screens, mixed currencies and units, package market rejection and valid/invalid seasonality data. [Compatibility tests](../artifacts/android-budget-compat-verified.log).

Current product prices, history, market comparisons and maps share market/presentation/unit filters. City classifications are visible. Numeric history tables sort descending. Rapid sequential filter changes preserve explicit user selections even when an older request is pending.

Market price lists expand to all compatible identities. Market A/B/Colombia and insumo comparisons retain matched product, package and units, explain unmatched rows and show category and overall percentage differences. Original PDF and read-only Excel viewers preserve page/worksheet/row links, zoom, pagination and downloads. Credits navigation is exercised across all five bottom tabs. Product transport calculation has been removed.

Input catalogs use ordered covering indexes and a bounded five-minute cache. The final measured departmental response returned 4,294 identities in 8.01s; municipal response returned 2,349 in 5.35s. The two temporary thin indexes were removed after both covering replacements were verified valid and ready; every other index stayed unchanged and representative query plans use the covering indexes ([cleanup evidence](../artifacts/input-catalog-index-cleanup.json)). Independent verification covered 60 identities, 20 categories and 1,678 published rows, including 35 exact prior-month values and 25 missing-month cases. Historical detail validation retained 15 locations where the recent window has only one. Broad uncached all-history national comparisons can still be slow on the small B1ms database; no rows are silently truncated.

Mi Finca saves the actual farm point through GPS or map placement. The active farm, exact pin, name, coordinate provenance and existing budgets survive reload. Choosing a reference municipality does not create or move a farm pin. Weather requests retain six-decimal coordinates end to end; climate layers keep their source resolution and municipal statistics remain labeled as municipal references.

## Simulator validation

Android: 29 distinct cases passed across iterative installed-app runs on Pixel 9 / Android 17 (API37), using the actual Android WebView. The four affected filter/map/farm cases passed on release `8f037b24`; case 26 passed on `81dece54` and verified 153 historical months, all January 2019 and 2020 rows and totals, source quantity records, visible filters, chart scrolling and native back. Zero JavaScript errors occurred in those final runs. Original local settings and network states were restored. [Cumulative Android audit](../artifacts/android-validation-notes.md) identifies each case, release and artifact; it does not imply that all 29 ran together on the final release. Final cases 26, 28 and 29 passed on `e34a99b1`, validating the single all-month selector, unified catalog and original Excel viewer, and actual touchscreen home suggestions with exact USD/city selection links and hardware back. The first home-back attempt was inconclusive; its focused retry passed, and both artifacts remain. Original settings and radios were restored. The user-reported stale-card painting was reproduced exactly on release `81dece54`: Android screen captures showed coffee content over tomato while WebView/CDP pixels, DOM geometry, hit targets and API data were correct. Disabling hover transforms and adding paint containment separately did not fix it. Restarting only the dedicated Pixel 9 with host graphics and Vulkan disabled resolved the exact problem on the unchanged release, with no injected CSS. The active renderer is Apple M4 / Metal rather than SwiftShader. The original data and other running emulator were preserved. Native scrolling, save/back navigation and 48-card expansion were visually rechecked. [Before](../artifacts/android-catalog-reproduction-81dece54/27-native-down-0.png), [after](../artifacts/android-catalog-host-renderer-81dece54/27-tomato-settled-after-load-more.png). The reproducible [emulator launcher](../apps/android/run-emulator.sh) refuses to replace another running device. Touch hover cleanup in the web code is separate from this verified emulator fix.

iOS: the final unified-catalog suite passed on a separate AgroAmigo QA iPhone 17 Pro / iOS 26.1 simulator against `e34a99b1`: all 24 initial cards scrolled both ways, 48-card expansion, saved/reference identity, currency filters, exact detail values, visible back links, 800 retained monthly prices beginning in 1960, and all 24 months of 2019/2020 supply. Screenshots were visually inspected. [Final catalog log](../artifacts/ios-catalog-final-flow-e34a99b1.log). The final complete suite also passed on the dedicated QA simulator against `e34a99b1`: 21 numbered groups and 25 screenshots covering price/classification, descending table, map popup, PDF, both Mora packages, Excel sheets/pagination, all five credits tabs, market/Colombia and insumo comparisons, municipal filters, regional links, USD source filtering, history to 1960, exact farm coordinates and reload, electricity, the international workbook, and historical 2019/recovered 2020 supply. The exact farm pin was obtained through the real CoreLocation path with the synthetic simulator fixture `1.912345, -76.123456`; GPS provenance and persistence passed. Simulator location was reset afterward. See [final native suite log](../artifacts/ios-full-final-gps-e34a99b1.log). The earlier `81dece54` run remains in [the preceding log](../artifacts/ios-full-validation.log). The additional data suite passed against the preceding `8f037b24` release: coffee kg/arroba arithmetic and negative-input handling, current daily data/filtering, private-offer units/discount/transport math and persistence, injected API failure/retry/recovery, and historical 2012 XLS native sharing ([data suite log](../artifacts/ios-data-validation.log)). Both suites restore the original local settings in teardown. Native screenshots were visually inspected; browser-only tests are identified separately. Canceled Next.js prefetches occasionally log a browser-navigation fallback during rapid test navigation; the subsequent screens load and their assertions pass.

Browser regression checks are separate: 12 comparison checks, six farm-location checks, and four final desktop/mobile delayed-request and popup-boundary checks passed. Production web build, scoped iOS static analysis and native navigation policy unit test passed. After integration testing, the normal `lib/main.dart` target was rebuilt, installed and launched successfully through XcodeBuildMCP; the dedicated AgroAmigo QA simulator is left running the regular app (process 55905). The regular app was also reinstalled on the earlier shared simulator without launching it or interrupting its other app. [Restoration and suite evidence](../artifacts/ios-validation-final.json).

Native iOS edge-swipe remains an open validation issue. An actual trusted tap opened the correct product, but the attempted left-edge swipes did not return to the catalog. Subsequent native input automation repeatedly failed to deliver taps (`noWindowsAvailable`), and enabling test semantics did not expose WebView accessibility targets. The unverified eager-gesture candidate was removed; the production native wrapper keeps its prior gesture configuration. The visible product back links pass. Diagnostic attempts are retained separately and are not counted as passing gesture tests.

## Navigation for maintainers

[Code navigation](CODE_NAVIGATION.md) maps the source adapters, immutable stores, publication views, API routes, shared React screens and native wrappers. [Ingestion operations](../pipelines/ingestion/README.md) documents deployment, remote configuration, revision handling, OCR, diagnostics and recovery. Validation artifacts are stored under `artifacts/` and are not deployment content.
